ZEIT_APP_NAME=Zeit Project API
ZEIT_ENV=dev
ZEIT_TIMEZONE=UTC

# Local default: SQLite persisted through the Docker /data volume.
ZEIT_DATABASE_URL=sqlite:////data/test.db

# Railway / production example:
# ZEIT_ENV=prod
# ZEIT_DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DBNAME

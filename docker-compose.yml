services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      ZEIT_DATABASE_URL: ${ZEIT_DATABASE_URL:-sqlite:////data/test.db}
    volumes:
      - zeit_data:/data

volumes:
  zeit_data:
